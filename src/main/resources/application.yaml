spring:
  application:
    name: spring-boot-db-access-lab
  datasource:
    url: jdbc:h2:file:./data/postdb;MODE=MySQL;AUTO_SERVER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console

springdoc:
  swagger-ui:
    path: /swagger
    operations-sorter: method
  default-produces-media-type: application/json
